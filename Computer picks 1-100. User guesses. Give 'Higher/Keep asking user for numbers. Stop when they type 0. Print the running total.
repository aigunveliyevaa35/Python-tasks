total = 0

while True:
    num = float(input("Enter one number: (0 for stop): "))
    
    if num == 0:
        break;
        
    total += num
    print(f"Cari cəmi: {total}")

print(f"Final cəmi: {total}")
